import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class MyDivider10 extends StatelessWidget {
  const MyDivider10({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 10.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider15 extends StatelessWidget {
  const MyDivider15({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 15.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider20 extends StatelessWidget {
  const MyDivider20({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 20.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider25 extends StatelessWidget {
  const MyDivider25({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 25.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider30 extends StatelessWidget {
  const MyDivider30({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 30.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider5 extends StatelessWidget {
  const MyDivider5({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 5.h,
      color: Colors.transparent,
    );
  }
}

class MyDivider2 extends StatelessWidget {
  const MyDivider2({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 2.h,
      color: Colors.transparent,
    );
  }
}
